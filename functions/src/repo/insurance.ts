import { InsuranceProvider } from "../types";

export const addInsuranceProviderForPatient = (params: InsuranceProvider) => {
  // first check to see if the provider exists
  // if so, add it in
  // else just do nothing 
};
